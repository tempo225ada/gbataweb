{% extends 'admin.html.twig' %}

{% block body %}
    <div class="container">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">Identifiant</th>
                <th scope="col">Commune</th>
            </tr>
            </thead>
    {% for commune in communes %}
            <tbody>
            <tr>
                <td> {{ commune.id }} </td>
                <td> {{ commune.nom }} </td>
                <td> <a href="{{ path('admin_commune_edit',{
                        id:commune.id
                    })
                    }}">Modifier</a> </td>

            </tr>
            </tbody>
    {% endfor %}
        </table>
    </div>
{% endblock %}