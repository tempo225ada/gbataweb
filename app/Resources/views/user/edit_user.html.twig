{% extends 'admin.html.twig' %}

{% block body %}
    <div class="container mt-4">
        {{ form_start(form) }}

            <button type="submit">Modifier</button>

        {{ form_end(form) }}
    </div>
{% endblock %}